<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GetGoing" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/color_background_new</item>
        <item name="colorButtonNormal">@color/color_button_background</item>
        <item name="colorOnSurface">@color/color_button_background</item>
        <item name="android:textColorPrimary">@color/text_light_primary</item>
        <item name="android:textColorSecondary">@color/text_light_primary</item>
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.App.MaterialAlertDialog</item>

    </style>

    <style name="ThemeOverlay.App.MaterialAlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="colorPrimary">@color/primaryButton</item>
    </style>

    <style name="Theme.GetGoing" parent="Base.Theme.GetGoing" />

    <style name="AppTheme" parent="android:Theme.Material.Light.NoActionBar" />

</resources>
